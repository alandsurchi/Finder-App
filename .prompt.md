# 🚀 MEGA PROMPT: Build a Full Flutter + Firebase Production App (Step-by-Step)

## PURPOSE
Guide me step-by-step to transform my existing Flutter application into a fully functional, production-ready app using Firebase (Authentication, Firestore, and Cloud Messaging). The app must use real-time data, clean architecture, proper state management, and secure backend rules.

You MUST act like a senior software engineer guiding a junior developer.

---

## GLOBAL RULES (VERY IMPORTANT)
- Do NOT skip steps
- Do NOT assume anything is already configured
- Always proceed step-by-step
- After EACH major step:
  1. Explain briefly
  2. Provide full working code
  3. Tell me where to place the code
  4. Tell me what to test
  5. WAIT for my confirmation before continuing

- Keep explanations SHORT but CLEAR
- Focus on IMPLEMENTATION, not theory
- Code must be CLEAN, MODULAR, and SCALABLE

---

## MANUAL STEP PROTOCOL (MANDATORY)
Whenever a manual step is required, you MUST stop and write EXACTLY:

STOP: Please do this manually:

Then explain the required action clearly.

Examples:
- Create Firebase project
- Add google-services.json / GoogleService-Info.plist
- Enable Authentication
- Enable Firestore
- Enable Cloud Messaging
- Set Firestore security rules

Then WAIT for my confirmation before continuing.

---

## REQUIRED ARCHITECTURE

You MUST enforce clean architecture:

- models/        → data models
- services/      → Firebase logic
- providers/     → state management (Provider or Riverpod ONLY)
- screens/       → UI screens
- widgets/       → reusable UI components

---

## STATE MANAGEMENT
Use ONLY ONE:
- Provider OR Riverpod (choose one and stay consistent)

Manage:
- Auth state
- Firestore data streams
- UI updates

---

## FIREBASE FEATURES TO IMPLEMENT

### 1. AUTHENTICATION SYSTEM
Implement Firebase Email/Password authentication:

Features:
- Sign Up
- Log In
- Log Out
- Persistent session

After sign up:
- Create user document in Firestore

User document must include:
- uid
- email
- createdAt

---

### 2. FIRESTORE DATABASE STRUCTURE (STRICT)

You MUST use this exact structure:

users/{userId}
posts/{postId}
chats/{chatId}
chats/{chatId}/messages/{messageId}
notifications/{notificationId}

Each document MUST include:
- createdAt (timestamp)
- userId (where applicable)

---

### 3. REMOVE MOCK DATA
- Remove ALL dummy/static data
- Replace everything with Firestore
- Use:
  - StreamBuilder → for real-time data
  - FutureBuilder → only if necessary

---

### 4. POSTING SYSTEM

Users can create posts stored in `posts`

Each post MUST include:
- userId
- title
- description
- createdAt

Behavior:
- Posts appear instantly in Home screen (real-time)
- Posts appear in Search screen
- Only owner can edit/delete posts

---

### 5. MESSAGING SYSTEM (REAL-TIME CHAT)

Structure:
- chats/{chatId}
- chats/{chatId}/messages/{messageId}

Requirements:
- 1-to-1 chat system
- Unique chatId per user pair

Each message MUST include:
- senderId
- receiverId
- text
- createdAt

Behavior:
- Real-time updates using StreamBuilder
- Messages sorted by timestamp

---

### 6. NOTIFICATIONS SYSTEM

Use Firebase Cloud Messaging (FCM)

Requirements:
- Save FCM token per user
- Store notifications in Firestore

Trigger notifications for:
- New messages
- Important user activity

---

### 7. FIRESTORE SECURITY RULES

You MUST implement secure rules:

Users can:
- Read/write their own data
- Modify only their own posts
- Access only their own chats

Prevent:
- Unauthorized access
- Access to other users’ private data

---

## CODE REQUIREMENTS
- Use best practices
- Separate logic from UI
- Use services for Firebase logic
- Use models for data structure
- Use providers for state

---

## OUTPUT FORMAT (EVERY STEP)

For each step, you MUST provide:

1. Short explanation
2. Full code
3. Where to place the code
4. What to test
5. WAIT for confirmation

---

## EXECUTION STYLE
- Build the app like a REAL production system
- Do NOT rush
- Do NOT give incomplete code
- Ensure each feature works before moving on

---

## START COMMAND

When I say:
"Start building the app step-by-step"

You MUST begin from:
Step 1 → Firebase Project Setup + Authentication

---

## FINAL GOAL
By the end, I should have:
- Fully working Flutter app
- Real-time Firestore backend
- Authentication system
- Posting system
- Messaging system
- Notifications system
- Clean, scalable architecture

This must be PORTFOLIO-LEVEL quality.
